import 'package:flutter/material.dart';

class AllColor {

    static const Color transparent = Colors.transparent;
    static Color? textFormText = Colors.grey[600];
    static Color? textFormBox = Colors.grey[300];
    static Color? textFormBoxFocus = Colors.blue.shade50;
    static Color? errorRed = Colors.red.shade50;

    static Color? primaryButtonColor = Colors.blue.shade400;
    static Color? primaryButtonColorDisabled = Colors.blue.shade200;

    static Color? toastErrorColor = Colors.red;
    static Color? toastSuccessColor = Colors.green;

    static Color? headingTextColor = Colors.black;


    static Color? primaryFocusColor = const Color.fromRGBO(36, 110, 159, 1);

    static Color? selectedIconColor = const Color.fromRGBO(58, 162, 230, 1);

}